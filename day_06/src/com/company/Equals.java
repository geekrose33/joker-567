package com.company;

/**
 * @author Joker_Dong
 * @date 2020-5-17 15:34
 */

public class Equals {
    public static void main(String[] args) {

    }
/*    public boolean equals_exam(Object anobject){
        char[] value = new char[];
        if (this == anobject){
            return true;
        }//跟自己进行一次比较
            if (anobject instanceof String){ // 传入的 anobject是否属于String类型
            String anotherString = (String)anobject; // anobject 强制转换String类型
            int n = value.length; // 第一个字符串长度
            if (n == anotherString.value.length){
                char v1[] = value; // 赋值
                char v2[] = anotherString.value; // 赋值
                int i = 0;
                while (n-- != 0){//v1和v2中每一个元素逐一比较
                    if (v1[i] != v2[i])
                        return false;
                    i++;

                }

            }

        }

        return false;

    }*/
}
